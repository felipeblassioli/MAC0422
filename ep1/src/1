#include <stdio.h>
#include "simulator.h"
#include "linked_list.h"

ProcessControlBlock *sjb_choose(LinkedList *ready_processes, LinkedList *running_processes){
	// choose the minium burst time in the ready_processes
	return (ProcessControlBlock *)NULL;
}

ProcessControlBlock *srtn_choose(LinkedList *ready_processes, LinkedList *running_processes){
	// choose the minimum burst time from the ready_processes and the running_processes
	return (ProcessControlBlock *)NULL;
}

ProcessControlBlock *rr_choose(LinkedList *ready_processes, LinkedList *running_processes){
	return (ProcessControlBlock *)NULL;
}

ProcessControlBlock *fcfs_choose(LinkedList *ready_processes, LinkedList *running_processes){
	return proc;
}
